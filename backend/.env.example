# ============================================================================
#  Brisas Marinas — Configuración de Entorno
#  Copiar este archivo como .env y ajustar los valores.
# ============================================================================

# --- Servidor ---
NODE_ENV=development
PORT=3000

# --- Base de Datos PostgreSQL ---
DB_HOST=dpg-d88ag7h9rddc73baopk0-a.oregon-postgres.render.com
DB_PORT=5432
DB_NAME=brisas_marinas
DB_USER=brisas_marinas_user
DB_PASSWORD=flwuHpCZh9DA9o0xa8rbNjVDhdBzw6Z4

# --- Autenticación JWT ---
# Generar una cadena segura aleatoria de al menos 32 caracteres en producción.
JWT_SECRET=cambiar-esta-clave-secreta-en-produccion-min-32-caracteres
JWT_EXPIRES_IN=8h

# --- CORS (orígenes permitidos, separados por coma) ---
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5500,http://127.0.0.1:5500,http://localhost:8080

# --- Rate Limiting ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# --- Usuario administrador inicial (creado por seed:users) ---
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
[email protected]
ADMIN_FULL_NAME=Baldura María Petro Pérez
